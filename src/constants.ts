import * as vscode from 'vscode';

export const ASSET_FOLDER = 'assets';

export const ASSET_PATH_ICONS: Record<string, vscode.ThemeIcon> = {
    sprites: new vscode.ThemeIcon('symbol-namespace'),
};
